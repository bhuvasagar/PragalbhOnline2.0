# Backend API URL
# Leave empty or set to /api to use local proxy (vite.config.ts)
# For production, set to your deployed backend URL (Render/GCP/AWS). Example:
# VITE_API_BASE_URL=https://pragalbh-backend.onrender.com
VITE_API_BASE_URL=

# Optional: Tell backend which frontend origin to allow (used by the backend CORS config)
# Set this to your frontend production origin (no trailing slash). Example:
# VITE_FRONTEND_ORIGIN=https://your-frontend.vercel.app
# The backend reads this value into its CORS_ORIGIN environment variable.
# Note: Browsers enforce CORS; frontend cannot bypass it—this is just a helper value.
VITE_FRONTEND_ORIGIN=
